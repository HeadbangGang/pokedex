import React from 'react'
import PropTypes from 'prop-types'

export const PokemonDataChart = ({ data }) => {
    PokemonDataChart.propTypes={
        data: PropTypes.object
    }
    return (
        <div>
            pokemon stat chart here
        </div>
    )
}